apiVersion: v1
kind: ConfigMap
metadata:
  name: frontend-config
  namespace: atonixcorp
data:
  REACT_APP_API_URL: "https://api.atonixcorp.com/api"
  REACT_APP_ENVIRONMENT: "production"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: atonixcorp
  labels:
    app: frontend
    component: ui
spec:
  replicas: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
        component: ui
    spec:
      containers:
      - name: frontend
        image: atonixcorp/frontend:latest
        ports:
        - containerPort: 80
        envFrom:
        - configMapRef:
            name: frontend-config
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: atonixcorp
  labels:
    app: frontend
    component: ui
spec:
  ports:
  - port: 80
    targetPort: 80
    name: http
  selector:
    app: frontend